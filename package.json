{
  "name": "daily-check-in",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Daily Check-In with local email delivery via Mailpit.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
